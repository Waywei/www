{% from "_formhelper.html" import render_field %}
extends base/layout
block title
  title {{finished.title}} | {{config.BASETITLE}} 
block main
    #main
      .breadcrumb
        a(href="/") {{config.BASETITLE}}
        |  >  
        span {{finished.title}}
      #content
        .box
          .box-title
            | {{finished.title}}
          .box-content
            div(style="padding:10px 20px")
              h2 {{finished.content}}
      #aside
        include base/my.jade
